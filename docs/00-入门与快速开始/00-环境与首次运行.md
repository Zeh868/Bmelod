# 00 环境与首次运行

> **本文职责**：10 分钟内完成拉取代码 → 构建 → 运行示例或单元测试（无需真实硬件）。

本文帮助你在 **10 分钟内**完成：拉取代码 → 构建 → 运行示例或单元测试。无需真实硬件。

## 环境要求

| 场景 | 工具 |
|------|------|
| PC 本地模拟 | CMake 3.20+、C 编译器（MSVC / GCC / Clang） |
| 单元测试 | 同上 |
| QEMU 示例 | `arm-none-eabi-gcc`、`qemu-system-arm` |
| Windows 构建 | 推荐 Visual Studio 生成器或 Ninja |

## 1. 克隆与目录

```bash
git clone <repo-url> bmelod-baremetal
cd bmelod-baremetal
```

框架源码在 `Source/`、`include/`；平台 Port 在 `portable/`；示例在 `Demo/`。

## 2. 运行第一个示例（PC 模拟）

以 `core_sensor` 为例，演示事件、内存池与模块生命周期：

```bash
cmake -B build/demo/manual/core_sensor -S Demo/core_sensor -DBMELOD_BAREMETAL_DIR=../..
cmake --build build/demo/manual/core_sensor
./build/demo/manual/core_sensor/core_sensor          # Linux/macOS
build\demo\manual\core_sensor\Debug\core_sensor.exe  # Windows MSVC
```

成功时终端会打印模块初始化与温度事件处理日志。

**PowerShell 快捷脚本**：

```powershell
.\tools\demo\run_native.ps1 core_sensor
```

## 3. 运行单元测试

```bash
cmake -B build -DBM_BUILD_TESTS=ON \
  -DBM_ENABLE_SHELL=ON \
  -DBM_ENABLE_HRT=ON \
  -DBM_ENABLE_TICKER=ON \
  -DBM_ENABLE_EXEC=ON \
  -DBM_ENABLE_SYNC=ON
cmake --build build --config Debug
ctest --test-dir build -C Debug --output-on-failure
```

全部通过即表示核心与混合域组件在 `native_sim` HAL 上行为正常。

## 4. QEMU 示例（可选）

```bash
.\tools\demo\run_qemu.ps1 interrupt_demo
# 或手动：
# cmake -B build/demo/qemu/interrupt_demo -S Demo/interrupt_demo ...
```

## 5. 创建自己的应用

最小步骤：

1. 复制 `Demo/core_sensor/` 为模板。
2. 编写应用侧 `bm_config.h`（容量上限 + `BM_CONFIG_ENABLE_*` 组件开关）。
3. Include Path：`你的工程/`（`bm_config.h`）+ 框架 `include/`（**一条**）。
4. 应用代码：`#include "bmelod.h"`；板级 HAL 按需 `#include "bm_hal_*.h"`。
5. CMake：`bmelod_configure` + `bmelod_link`，或手动链接 `bm_core`、`bm_module` 等目标与 `bm_hal_*` 后端。
6. 在 `main()` 中：`bm_event_reset()` → `bm_module_init_all()` → 主循环 `bm_event_process()`。

详见 [../02-构建与工具链/01-CMake选项与bm_config](../02-构建与工具链/01-CMake选项与bm_config.md)、[../01-应用开发/01-Demo示例与运行路径](../01-应用开发/01-Demo示例与运行路径.md) 与 [../01-应用开发/02-main骨架与数据流](../01-应用开发/02-main骨架与数据流.md)。

## 6. 下一步读什么

完整索引与推荐阅读路径：[docs/README.md](../README.md)。

| 目标 | 文档 |
|------|------|
| 从零搭 `main` | [../01-应用开发/02-main骨架与数据流](../01-应用开发/02-main骨架与数据流.md) |
| 事件与模块 | [../01-应用开发/04-事件模块与通道](../01-应用开发/04-事件模块与通道.md) |
| 混合域接线 | [../01-应用开发/05-混合域接线](../01-应用开发/05-混合域接线.md) |
| 移植 MCU | [../03-移植与IDE集成/02-挂库到现有工程](../03-移植与IDE集成/02-挂库到现有工程.md) |
