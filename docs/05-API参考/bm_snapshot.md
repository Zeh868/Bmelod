# bm_snapshot — 已退役

> **此原语已由 `bm_bus` 收编（Phase 1）。**
> 请使用 `bm_bus` LATEST 模式替代：`BM_BUS_DEFINE(name, type, 3u, 1u, BM_BUS_LATEST)`。
> 详见 `include/bm/core/bm_bus.h` 及迁移测试 `tests/unit/test_bus_migration.c`。

原头文件 `include/bm/hybrid/bm_snapshot.h` 已删除。
